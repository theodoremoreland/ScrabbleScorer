import { ScoringAlgorithmName } from "../types/types";

interface ScoreQueryParams {
    word: string;
    scoringAlgorithmName: ScoringAlgorithmName;
}

export default ({ word, scoringAlgorithmName}: ScoreQueryPrams ) => {
    
}